/**
 * @swagger
 * components:
 *   schemas:
 *     MessageDto:
 *       type: object
 *       properties:
 *         message:
 *           type: string
 *           description: The message from server
 */

export interface MessageDto {
    message: string
}